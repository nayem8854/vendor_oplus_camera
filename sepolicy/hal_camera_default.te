# Baseline only. Device tree already has samurai-specific camera HAL rules
# (device/realme/samurai/sepolicy/vendor/hal_camera_default.te).
#
# Do not add binder/app cross-domain allows here without a device denial
# log — neverallow failures will break the sepolicy build.
